# JSON Easy Editor

A React + TypeScript JSON editor with tree view, input/output panels, and preset support.

**Live demo:** https://guitorte.github.io/json/

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies: `npm install`
2. Run the app: `npm run dev`
